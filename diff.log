diff --git a/av1/encoder/interp_search.c b/av1/encoder/interp_search.c
index 0066c3543..487a323d4 100644
--- a/av1/encoder/interp_search.c
+++ b/av1/encoder/interp_search.c
@@ -649,7 +649,7 @@ int64_t av1_interpolation_filter_search(
     const TileDataEnc *tile_data, BLOCK_SIZE bsize,
     const BUFFER_SET *const tmp_dst, const BUFFER_SET *const orig_dst,
     int64_t *const rd, int *const switchable_rate, int *skip_build_pred,
-    HandleInterModeArgs *args, int64_t ref_best_rd) {
+    HandleInterModeArgs *args, int64_t ref_best_rd, int do_search) {
   const AV1_COMMON *cm = &cpi->common;
   const InterpSearchFlags *interp_search_flags = &cpi->interp_search_flags;
   const int num_planes = av1_num_planes(cm);
@@ -709,6 +709,9 @@ int64_t av1_interpolation_filter_search(
   *rd = RDCOST(x->rdmult, *switchable_rate + rd_stats.rate, rd_stats.dist);
   x->pred_sse[ref_frame] = (unsigned int)(rd_stats_luma.sse >> 4);
 
+  if (!do_search) {
+    return 0;
+  }
   if (assign_filter != SWITCHABLE || match_found_idx != -1) {
     return 0;
   }
diff --git a/av1/encoder/interp_search.h b/av1/encoder/interp_search.h
index 1ee26d11b..c4f422339 100644
--- a/av1/encoder/interp_search.h
+++ b/av1/encoder/interp_search.h
@@ -159,7 +159,7 @@ int64_t av1_interpolation_filter_search(
     const TileDataEnc *tile_data, BLOCK_SIZE bsize,
     const BUFFER_SET *const tmp_dst, const BUFFER_SET *const orig_dst,
     int64_t *const rd, int *const switchable_rate, int *skip_build_pred,
-    HandleInterModeArgs *args, int64_t ref_best_rd);
+    HandleInterModeArgs *args, int64_t ref_best_rd, int do_search);
 
 /*!\endcond */
 #ifdef __cplusplus
diff --git a/av1/encoder/rdopt.c b/av1/encoder/rdopt.c
index fbffe07cc..0dcd1ba2a 100644
--- a/av1/encoder/rdopt.c
+++ b/av1/encoder/rdopt.c
@@ -2786,7 +2786,7 @@ static int64_t handle_inter_mode(
     // Determine the interpolation filter for this mode
     ret_val = av1_interpolation_filter_search(
         x, cpi, tile_data, bsize, &tmp_dst, &orig_dst, &rd, &rs,
-        &skip_build_pred, args, ref_best_rd);
+        &skip_build_pred, args, ref_best_rd, 0);
 #if CONFIG_COLLECT_COMPONENT_TIMING
     end_timing(cpi, interpolation_filter_search_time);
 #endif
